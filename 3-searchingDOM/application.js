$(document).ready(function() {
	//selecting all the list items direct child of #tours
	$("#tours > li")

	//selecting all the tours from Asia and all the tours that are OnSale
	$(".asia, .sale")

	//selecting only the first url
	$("#torus li:first")
	
	//selecting only even rows to add striping colour style
	$("#tours > li:even")
});
